{
  "extends": ["eslint:recommended", "google"],
  "rules": {
    // Additional, per-project rules...
    "semi": ["error", "always"],
    "quotes": ["error", "double"]
  },
  "plugins": [    "json",    "markdown"  ]
}